import 'package:flutter/material.dart';

class ItemAcademicLevel extends StatelessWidget {
  const ItemAcademicLevel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
